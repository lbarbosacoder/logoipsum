document.addEventListener('DOMContentLoaded', () => {
  console.log("Página carregada com sucesso!");

  // Mensagem de boas-vindas (exemplo simples)
  setTimeout(() => {
    alert("Bem-vindo ao site da System Solutions!");
  }, 1000);
});